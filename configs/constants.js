export const MAX_PAGINATION = Number(process.env.MAX_PAGINATION);
export const DEFAULT_PAGINATION = Number(process.env.DEFAULT_PAGINATION);
export const CACHE_TIME_MILLIS = Number(process.env.CACHE_TIME_MILLIS);

export const AUTHOR = {
  name: process.env.AUTHOR_NAME,
  lastname: process.env.AUTHOR_LASTNAME,
};
